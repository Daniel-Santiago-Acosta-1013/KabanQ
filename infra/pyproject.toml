[project]
name = "kabanq-infrastructure"
version = "1.0.0"
description = "AWS CDK infrastructure for KabanQ"
requires-python = ">=3.13"
dependencies = [
    "aws-cdk-lib>=2.180.0",
    "constructs>=10.0.0",
]

[tool.uv]
package = false

{
  "name": "kanbanq-monorepo",
  "private": true,
  "version": "1.0.0",
  "description": "KabanQ monorepo with React 19 + Vite frontend and Python backend",
  "workspaces": [
    "apps/frontend"
  ],
  "scripts": {
    "dev": "bun run --cwd apps/frontend dev",
    "build": "bun run --cwd apps/frontend build",
    "test": "bun run --cwd apps/frontend test"
  }
}
